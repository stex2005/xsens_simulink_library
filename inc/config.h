#ifndef _CONFIG_H
#define _CONFIG_H

#define SAMPLERATE_MAX          150     // max. samlperate in [Hz] for dimension of circular buffer only
#define RADIO_CHANNEL           19      // the radio channel to use
#define SENSORS_MAX             10      // max 10 sensors allowed
#define ASSUMED_WRAP_THRESHOLD  30000   // gap in sample index on which a index overflow is assumed

#endif // _CONFIG_H
